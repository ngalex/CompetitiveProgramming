#include <bits/stdc++.h>
using namespace std;

int main() {
    int m,n,num;
    vector <int> A,B;

    cin>>m>>n;
    while (m--) {
        cin>>num;
        A.push_back(num);
    }
    while(n--){
        cin>>num;
        B.push_back();
    }

}
