#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    vector<char> v;
    char a;
    while(n--){
        cin>>a;
        v.push_back(a);
    }
    cout<<v.size();
    return 0;
}
