#include <bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int t,i,j,p;
    bool flag;
    string s,op;
    vector<string> sums;
    vector<string> rest;
    cin>>t;
    while(t--){
        cin>>s;

    }
    return 0;
}

void calcularParentesis(int i,int j, string st){


}

/*if(s[i]=='(') flag=1;
            if(flag){
                op+=s[i];
                if(s[i]==')')flag=0;
            }
            else{
            }*/
/*
        for(p=0;i<sums.size();i++){
            cout<<sums[i]<<"\n";
        }
        for(p=0;i<rest.size();i++){
            cout<<rest[i]<<"\n";
        }*/
